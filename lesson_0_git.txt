mkdir ~/projects/cdg-practice/ 
cd ~/projects/cdg-practice/ 
git init

touch hello.rb - create document
echo "puts 'Hello World'" >> hello.rb
cat hello.rb - add line in file - hello.rb
ruby hello.rb - run / read file - hello.rb

git status - сheck status  Staged /  Unstaged

git add hello.rb

git rm --cached ..source to file - make file unstaged

git commit -m "Initial commit"

git branch - show all branches in project

git checkout -b change-hello-rb - Switched to a new branch 'change-hello-rb' (Create new branch)

git branch 

echo "puts 'Hello CDG students'" >> hello.rb // Add line in file hello.rb in branch "change-hello-rb"

ruby hello.rb // launch (run) hello.ru

git checkout main // Go to branch - master

cat hello.rb // show text content in hello.rb

git merge change-hello-rb // join (combine) branch change-hello-rb with branch master

git branch -D change-hello-rb // (-d) - flag delete branch

git remote add origin https://github.com/wwworm-bot/cdg-practice.git // add remote repository on github

git branch -M main // set branch Main

git push -u origin master // -U (flag combine local branch with github)




